/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
      domains: [
          'upload.wikimedia.org',
          'www.growthmentor.com',
          'images.unsplash.com',
      ],
  },
  
};

export default nextConfig;
